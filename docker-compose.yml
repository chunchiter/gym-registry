services:
  postgres:
    image: postgres:16
    container_name: gym_db
    environment:
      POSTGRES_DB: gymdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - gym_data:/var/lib/postgresql/data

volumes:
  gym_data:
